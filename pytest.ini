[pytest]
testpaths = api/tests
markers =
    unit: fast isolated tests for pure helpers and serializers.
    integration: backend route and cross-module integration coverage.
    e2e: browser-driven end-to-end coverage against the running stack.
    recovery: failure-path and degraded-state coverage.
    perf: moderate latency and concurrency smoke coverage.
addopts = -ra
