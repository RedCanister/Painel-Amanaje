name: Legacy Kubernetes Workflow

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: self-hosted
    steps:
      - name: Show migration notice
        shell: powershell
        run: |
          Write-Host "Use .github/workflows/build-and-deploy-1.yml for active Kubernetes builds and deployments."
